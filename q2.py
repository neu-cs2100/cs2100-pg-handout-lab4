from typing import List, TypeVar

"""
Please implement the following functions using Pythonic list indexing (no for loops):
"""

T = TypeVar('T')

def first_n(items: List[T], n: int) -> List[T]:
    """Returns the first n elements of the list of items.
    If there are fewer than n elements, returns the entire list.
    """
    pass

def last_n(items: List[T], n: int) -> List[T]:
    """Returns the last n elements of the list of items.
    If there are fewer than n elements, returns the entire list.
    """
    pass

def within_range(items: List[int], min: int, max: int) -> List[int]:
    """Returns a list of the items in the provided list which are 
    at least the min value and at most the max value.
    """
    pass